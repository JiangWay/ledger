# Any property name starting with "info.",will be used by "/info"
info.app.name=Ledger
info.app.description=LEDGER APP
info.app.version=1.0.0

# Use wildcard "*" to expose all endpoints
# Can also expose individual endpoints with a comma-delimited list
management.endpoints.web.exposure.include=*

# Exclude individual endpoints with a comma-delimited list 完全排出 就算登入後也不顯示
# management.endpoints.web.exposure.exclude=health,info

# Base path for actuator endpoints
management.endpoints.web.base-path=/actuator

# Security Properties
#spring.security.user.name=wayne
#spring.security.user.password=0312

#
# Define my crazy properties
#
coach.name=Mickey Mouse
team.name=The Mouse Club


# HTTP server port
server.port=8080

# Context path of the application
server.servlet.context-path=/wayne-ledger-app

# Default HTTP session time out(預設30m)
server.servlet.session.timeout=15m


# DataSource配置
spring.datasource.url=jdbc:mariadb://localhost:3306/employee_directory
spring.datasource.username=hbstudent
spring.datasource.password=hbstudent
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver


